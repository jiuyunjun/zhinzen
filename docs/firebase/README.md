# Firebase 文档

Firebase / GCP 配置与运维文档目录（占位）。

- 安全规则现状与待补强项见 [`firebase/firestore.rules`](../../firebase/firestore.rules)
  与 [`firebase/database.rules.json`](../../firebase/database.rules.json)（均为 v0 baseline）。
- 安全模型方向见 [`design.md` §15](../../design.md) 与 [`agents.md` §6](../../agents.md)。
- 项目初始化（`firebase use`、环境、App Check）将在接入真实后端时在此记录。
