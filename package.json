{
  "name": "zhinzen",
  "version": "0.0.0",
  "private": true,
  "description": "Zhinzen — 无账号的实时位置共享 / accountless live location sharing",
  "workspaces": [
    "packages/*",
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @zhinzen/web",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
