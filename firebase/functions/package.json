{
  "name": "@zhinzen/functions",
  "version": "0.0.0",
  "private": true,
  "description": "Zhinzen Cloud Functions — room creation, write validation, cleanup (design.md §6.1). Deployed independently; not part of the root npm workspace.",
  "type": "module",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
